const car={
    model : "Tesla Model X",
    color : "Red",
    wheelSize : 20,
    type : "Sedan",
    infocar : function(){
        return this.model + " "+this.color;
    }
}
let text = "sandesh yesane";
text.search(sandesh);
let caeinfo = "the latest ford endavour";
carinfo.search(ford);